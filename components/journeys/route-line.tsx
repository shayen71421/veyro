"use client";
import { useLayoutEffect, useRef } from "react"; import gsap from "gsap";
export function RouteLine({ compact = false }: { compact?: boolean }) { const ref = useRef<SVGPathElement>(null); useLayoutEffect(() => { if (!ref.current) return; const reduce = matchMedia("(prefers-reduced-motion: reduce)").matches; const length = ref.current.getTotalLength(); const context = gsap.context(() => { gsap.fromTo(ref.current, { strokeDasharray: length, strokeDashoffset: length }, { strokeDashoffset: 0, duration: reduce ? .01 : 1.15, ease: "power2.inOut" }); }); return () => context.revert(); }, []); return <svg className={compact ? "route-line compact" : "route-line"} viewBox="0 0 320 55" role="img" aria-label="Metro route"><circle cx="18" cy="28" r="7"/><path ref={ref} d="M25 28 C85 28 88 10 145 15 S225 50 295 28"/><circle cx="302" cy="28" r="7"/></svg>; }
