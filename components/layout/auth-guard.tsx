"use client";
import { useEffect } from "react"; import { usePathname, useRouter } from "next/navigation"; import { LoaderCircle } from "lucide-react"; import { useAuth } from "@/features/auth/auth-context"; import { BrandMark } from "@/components/ui/brand-mark";
export function AuthGuard({ children }: { children: React.ReactNode }) { const { user, loading } = useAuth(); const router = useRouter(); const path = usePathname(); useEffect(() => { if (!loading && !user) router.replace(`/login/?next=${encodeURIComponent(path)}`); }, [loading, user, router, path]); if (loading || !user) return <div className="screen center"><BrandMark size={54} label="Veyro"/><LoaderCircle className="animate-spin text-accent mt-4"/><p className="muted mt-3">Opening Veyro…</p></div>; return children; }
